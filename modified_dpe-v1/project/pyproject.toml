[project]
name = "project"
version = "0.1.0"
description = "Dictionary Based Pattern Entropy framework for Causal Inference."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv.workspace]
members = [
    "causal-method-env",
]
